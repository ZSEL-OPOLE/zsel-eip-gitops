apiVersion: v1
kind: Secret
metadata:
  name: user-ad-admin-secret
  namespace: identity-system
  labels:
    app.kubernetes.io/name: user-ad
    app.kubernetes.io/component: samba-ad-dc
    app.kubernetes.io/part-of: user-services
type: Opaque
stringData:
  # ⚠️ PLACEHOLDER - MUST BE REPLACED WITH SEALEDСECRET!
  # Generate strong password: openssl rand -base64 32
  # Follow workflow in: gitops/SECURITY-SEALED-SECRETS.md
  admin-password: "REPLACE_WITH_SEALED_SECRET"
