apiVersion: apps/v1
kind: Deployment
metadata:
  name: freeradius-user-wifi
  namespace: identity-system
  labels:
    app.kubernetes.io/name: freeradius-user-wifi
    app.kubernetes.io/component: radius-auth
    app.kubernetes.io/part-of: user-services
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: freeradius-user-wifi
  template:
    metadata:
      labels:
        app.kubernetes.io/name: freeradius-user-wifi
        app.kubernetes.io/component: radius-auth
    spec:
      containers:
      - name: freeradius
        image: freeradius/freeradius-server:3.2-alpine
        imagePullPolicy: IfNotPresent
        
        env:
        - name: RADIUS_BIND_PASSWORD
          valueFrom:
            secretKeyRef:
              name: freeradius-user-wifi-secrets
              key: radius-bind-password
        
        - name: WIFI_RADIUS_SECRET
          valueFrom:
            secretKeyRef:
              name: freeradius-user-wifi-secrets
              key: wifi-radius-secret
        
        - name: CAPTIVE_PORTAL_RADIUS_SECRET
          valueFrom:
            secretKeyRef:
              name: freeradius-user-wifi-secrets
              key: captive-portal-radius-secret
        
        ports:
        - name: radius-auth
          containerPort: 1812
          protocol: UDP
        - name: radius-acct
          containerPort: 1813
          protocol: UDP
        - name: radius-coa
          containerPort: 3799
          protocol: UDP
        
        volumeMounts:
        - name: ldap-config
          mountPath: /etc/raddb/mods-available/ldap
          subPath: ldap
        - name: clients-config
          mountPath: /etc/raddb/clients.conf
          subPath: clients.conf
        
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "radtest test test localhost 0 testing123 || exit 0"
          initialDelaySeconds: 30
          periodSeconds: 30
        
        readinessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "radtest test test localhost 0 testing123 || exit 0"
          initialDelaySeconds: 10
          periodSeconds: 10
        
        resources:
          requests:
            memory: "1Gi"
            cpu: "1000m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
      
      volumes:
      - name: ldap-config
        configMap:
          name: freeradius-user-wifi-ldap-config
      - name: clients-config
        configMap:
          name: freeradius-user-wifi-clients-config
