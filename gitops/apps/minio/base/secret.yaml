apiVersion: v1
kind: Secret
metadata:
  name: minio-root-credentials
  namespace: storage-system
type: Opaque
stringData:
  MINIO_ROOT_USER: "CHANGE_ME_minio_root_user_PLACEHOLDER"  # Replace with SealedSecret
  MINIO_ROOT_PASSWORD: "CHANGE_ME_minio_root_password_PLACEHOLDER"  # Replace with SealedSecret (min 8 chars)
